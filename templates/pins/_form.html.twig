{% form_theme form 'styles/pin_form.html.twig' %}
{{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="{{ submitButtonText | default('Create Pin') }}"
           class="btn btn-primary btn-lg col-12" formnovalidate>
    <!-- formnovalidate -->
{{ form_end(form) }}